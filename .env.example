# Required in current phase (schema/migration build)
DB_URL=postgresql+psycopg://user:password@localhost:5432/pdfa_workflow
DEFAULT_ACTOR=local_dev

# Deferred to later runtime phases (MCP/Flask service integration)
# MCP_HOST=127.0.0.1
# MCP_PORT=8010
# FLASK_HOST=127.0.0.1
# FLASK_PORT=5000
# MCP_BASE_URL=http://127.0.0.1:8010
